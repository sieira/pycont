import { INITIAL_AUTH_STATE } from './auth/constants'
import { INITIAL_ACCOUNTS_STATE } from './accounts/constants'

export const INITIAL_PYCONT_STATE = {
  auth: INITIAL_AUTH_STATE,
  accounts: INITIAL_ACCOUNTS_STATE,
}
